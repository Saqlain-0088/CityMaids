{
  "buildCommand": "cd citymaids-ui && npm install && npm run build",
  "outputDirectory": "citymaids-ui/dist",
  "installCommand": "echo 'skip'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
